# Used by src/main.rs
DATABASE_URL=postgres://rustpress:rustpress@localhost:5433/rustpress
BIND_ADDR=127.0.0.1:8082

# Logging configuration (optional, defaults to 'info')
# Set to 'debug' for more verbose logging, 'error' for less
# Examples:
#   RUST_LOG=info                    # Info level for all modules
#   RUST_LOG=debug                   # Debug level for all modules
#   RUST_LOG=rustpress=debug         # Debug level for rustpress module only
#   RUST_LOG=rustpress::web=trace    # Trace level for web handlers only
# RUST_LOG=info
