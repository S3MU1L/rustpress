
#[derive(Debug, Clone, Eq, PartialEq, Default)]
pub struct Coords {
    x: u32, // Coordinate from left (0) to right (max)
    y: u32, // Coordinate from top (0) to bottom (max)
    z: u32, // Layer on which the component exits
}

pub enum Component {
    Text{

    },

}

pub struct Page {

    pub components:
}

pub struct Site {

}
